# Build runner artifacts (local only)
build-logs/
BUILD_PROGRESS.md
PROMPT.md
build.sh

# Secrets
.env
secrets/

# Python
.venv/
__pycache__/
*.pyc
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Never commit a built index (doc text stays out of the repo)
index.json
*.index.json

# Partial eval runs. Only a full run's results are committed.
eval/raw_results.partial.jsonl

# Spin / Wasm build output
functions/.spin/
functions/target/
*.wasm

# Coverage and example venvs
.coverage
examples/**/.venv/
